we will have editor with manim where user will write those small card component code, so user write the individual component code like normal python function code in machine learning, by following a proper guide line, then based on user code the card component will be createded, in the editor there will be button option to test that code and after successfull compile, toggle to tensorRag board to see the visual component he just create. the code user write will be sync to s3 bucket. 